# 변경 이력

이 프로젝트는 별도의 버전 관리 도구(npm 등) 없이 정적 파일로 배포되는 사이트라, 릴리스는
이 파일과 git 태그로 기록합니다.

## v2.0.0 — 2026-09-10

기존 face-api.js 기반 · 성별 분리 · 라이트 뉴모피즘 테마 버전에서, 얼굴 분석 엔진과 UI를
전면적으로 새로 만든 릴리스입니다.

### 핵심 엔진 교체

- 얼굴 분석 라이브러리를 face-api.js에서 **MediaPipe Face Landmarker**(478 랜드마크)로 교체.
  나이 추정만 face-api.js를 보조로 유지(MediaPipe에 대응 모델 없음).
- 랜드마크에서 이마·눈매간격·코길이·입너비·턱선·얼굴형 6개 비율 특징을 뽑아 z-score
  정규화 후 거리 기반으로 매칭하는 방식으로 전환 (`js/faceFeatures.js`).
- `tools/precompute.html` 오프라인 도구로 표본 인물들의 특징을 미리 계산해
  `data/embeddings.json`에 저장 — 실시간 분석은 업로드한 사진 하나만 처리하면 되므로 빠름.
- 업로드된 이미지를 캔버스에 한 번 그려서 분석기에 넘기도록 변경 — 휴대폰 카메라 사진의 EXIF
  방향 태그를 일부 브라우저 디코딩 경로가 무시해 "화면엔 똑바른데 분석기엔 옆으로 누운 얼굴"이
  들어가 얼굴 인식에 실패하던 문제를 해결. 매우 큰 원본(수천 px)은 적당히 줄여 처리 속도도 개선.

### 표본 데이터

- 성별 분리를 없애고 하나의 통합 표본으로 재구성.
- 포브스 코리아 2026 50 Richest 중 CC 라이선스 사진을 확보한 실명 인물을 14명까지 확대
  (순위·자산·업적·사진 출처 포함). 나머지는 인구 통계를 두텁게 하는 익명 표본(50명)으로 유지.
- 실명 인물 매칭 로직을 "전체 62명 중 최유사"에서 "실명 있는 인물 중 최유사"로 변경 —
  익명 인물이 1위로 뽑히면 이름도 사진도 보여줄 게 없어 결과 카드가 통째로 안 뜨던 버그를 수정.

### 결과 화면 재설계

- 헤드라인 % 텍스트 블록을 없애고, 업로드한 사진 위에 매칭 인물 이름을 함께 표시하는
  인증마크 스타일 뱃지로 대체.
- "재벌 유형" 배지 신설 — 6개 항목 중 재벌 표본 평균에서 가장 크게 벗어난 항목 하나를 골라
  전략가형/오너형/승부사형 등 정체성 라벨을 부여 (MBTI류 성향 테스트 패턴 차용).
- 등급 판정 문구를 매칭 카드(사진 비교 + 육각 레이더 차트) 안으로 병합해 중복 문장 제거.
- 부위별 상세 분석에서 "재벌 표본 대비 백분위(막대)"와 "가장 닮은 인물"을 서로 다른 지표로
  명확히 분리 — 같은 자리에 다른 숫자 두 개가 있어 모순처럼 보이던 문제를 해결.
- AI 추정 나이·표정을 결과 카드가 아니라 업로드한 사진 바로 아래로 이동(사진 자체에 대한
  정보이므로).
- 표정 인식 임계값을 조정 — 진짜 무표정(~0.02)과 뚜렷한 미소(~0.35) 실측 분포 기준으로,
  은은하게 웃는 사진까지 무표정으로 오판하던 문제를 해결.

### 디자인 전면 개편

- 라이트 뉴모피즘 테마를 다크 퍼플 + 금박 점성술/관상학 테마로 전면 재도장.
- 기본 이모지 아이콘을 전부 Lucide(MIT) 라인 아이콘 SVG로 교체.
- 배경 패턴을 JS 캔버스에서 CSS 네이티브 반복 배경(세이가이하 물결무늬 SVG 타일)으로 교체 —
  페이지 길이가 늘어나도 찌그러지지 않고, 같은 반복 축을 좌우로 절반씩 어긋나게 짜 넣어
  진짜 청해파 문양처럼 인접 행이 자연스럽게 이어지도록 함.
- 로딩/전환 연출을 clip-path 원형 확장에서 균일한 opacity 페이드로 변경 — 원이 자라는 동안
  화면 귀퉁이에 배경이 깜빡이며 비치던 문제를 구조적으로 제거.
- 업로드 플레이스홀더를 손그림 스타일 아이콘에서 카메라 아이콘 + 글로우 + "+" 배지로 교체.
- 브라우저 기본 `alert()`를 테마에 맞춘 토스트 컴포넌트로 대체, 경고 색상(빨강 계열)으로
  일반 안내와 구분되도록 강조.

### SNS 공유

- 카카오톡/인스타그램/페이스북/X 공유 버튼 추가.
- "실제 이미지가 전달되는 공유"(공유하기·인스타그램, Web Share API)와 "링크만 전달되는 공유"
  (카카오톡·페이스북·X, 플랫폼 자체 제약)를 라벨로 명확히 구분해 배치.
- 공유 문구를 고정 텍스트에서 실제 결과(매칭 인물·유형·%) 기반 문구로 개인화.

### SEO / 마케팅

- `sitemap.xml`, `robots.txt` 정비(개발용 도구 경로 크롤링 차단 포함).
- 리디자인 이전 이미지를 쓰던 OG/트위터 카드 이미지를 현재 테마에 맞는 이미지로 교체.
- 파비콘/애플 터치 아이콘 신규 제작(이전까지 파비콘 없음).
- 구조화 데이터(JSON-LD, WebApplication) 추가.

### 기타 버그 수정

- 초기 로딩 시 업로드/초기화 버튼이 한동안 반응하지 않던 문제(스크립트 로딩 순서 문제) 수정.
- 초기화 버튼을 전체 새로고침에서 인앱 상태 초기화로 변경.
- 분석 실패 시 항상 "얼굴을 포함한 사진을 선택해주세요"로 뭉뚱그려 안내하던 것을, 실패
  지점별(모듈/모델 로딩 실패, 인식 실패, 표본 로딩 실패, 기타 오류)로 구분된 안내 문구로 개선.
- "개인정보를 절대 수집하지 않습니다" 문구가 실제로는 Google 애널리틱스/애드센스가 쿠키를
  쓰고 있어 부정확했던 것을 정확한 문구로 수정.

## v1.x — 그 이전

face-api.js 기반 얼굴 인식, 남녀 성별 분리 표본, 라이트 뉴모피즘 UI로 운영되던 초기 버전.
자세한 커밋 단위 이력은 `git log`를 참고하세요.
